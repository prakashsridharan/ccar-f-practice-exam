# Internal project memory - deliberately NOT published.
# The repo is public and GitHub Pages serves files from the repo root,
# so anything committed here is readable at ccar-f.pratechlabs.com/<file>.
CLAUDE.md
